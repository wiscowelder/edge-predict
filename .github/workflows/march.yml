name: Generate March Brackets

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  march:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate March brackets
        env:
          ODDS_API_KEY:    ${{ secrets.ODDS_API_KEY }}
          OPENWEATHER_KEY: ${{ secrets.OPENWEATHER_KEY }}
          BALLDONTLIE_KEY: ${{ secrets.BALLDONTLIE_KEY }}
        run: python brain.py --mode march

      - name: Save results
        run: |
          git config user.name  "EDGE Predict Bot"
          git config user.email "bot@edge-predict"
          git add data.json
          git diff --staged --quiet || git commit -m "March brackets generated $(date -u +%Y-%m-%d)"
          git push
